{% macro buildAccordion(data) %}
    {% for item in data %}
        <details>
            <summary>{{ item.title|raw }}</summary>
            {{ item.content|raw }}
        </details>
    {% endfor %}
{% endmacro %}
