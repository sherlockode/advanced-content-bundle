{% set formFields = form.fields %}
{% set formField = attribute(formFields, slug) %}

{% include '@SherlockodeAdvancedContent/ContentType/field_options_render.html.twig' with {options: formField.options} %}
