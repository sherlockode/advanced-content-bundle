{% extends '@SherlockodeAdvancedContent/layout.html.twig' %}
{% form_theme form '@SherlockodeAdvancedContent/Form/content_type.html.twig' %}
{% trans_default_domain 'AdvancedContentBundle' %}
{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('bundles/sherlockodeadvancedcontent/css/acb.css') }}">
{% endblock %}
{% block body %}
    {{ parent() }}
    <div class="container">
        <div class="row page-info">
            <div class="col-md-12">
                <h1>{{ 'content_type.edit'|trans }}</h1>
                <a href="{{ path('sherlockode_acb_content_type_list') }}" class="pull-right">{{ 'back_to_list'|trans }}</a>
            </div>
        </div>
        <hr>
        <div class="row">
            <div class="col-md-12">
                {{ form_start(form) }}
                {{ form_errors(form) }}
                <div class="row">
                    <div class="col-md-2">
                        {{ form_label(form.name) }}
                    </div>
                    <div class="col-md-10">
                        {{ form_widget(form.name) }}
                    </div>
                </div>
                <div class="row">
                    <div class="col-md-12">
                        {{ form_row(form.fields) }}
                    </div>
                </div>
                <div class="row">
                    <div class="col-md-12">
                        <button type="submit" class="btn btn-success pull-right">
                            {{ 'save'|trans }}
                        </button>
                    </div>
                </div>
                {{ form_widget(form._token) }}
                {{ form_end(form, {"render_rest": false}) }}
            </div>
        </div>
        <div class="field-options-remove-link" style="display: none">
            <a href="#" class="delete-choice"><i class="glyphicon glyphicon-trash"></i></a>
        </div>
        <hr />
    </div>
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    <script type="text/javascript" src="{{ asset('bundles/sherlockodeadvancedcontent/js/acb.js') }}"></script>
    <script type="text/javascript" src="{{ asset('bundles/sherlockodeadvancedcontent/js/speakingurl.min.js') }}"></script>
{% endblock %}
