{% extends '@SherlockodeAdvancedContent/layout.html.twig' %}
{% trans_default_domain 'AdvancedContentBundle' %}

{% block body %}
    {{ parent() }}
    <div class="container">
        <div class="row page-info">
            <div class="col-md-12">
                <h1>{{ 'page_type.label'|trans }}</h1>
                <a href="{{ path('sherlockode_acb_page_type_create') }}" class="float-end">{{ 'page_type.create'|trans }}</a>
            </div>
        </div>
        <div class="row">
            <div class="col-md-12">
                <table class="table">
                    <thead>
                    <tr>
                        <th>#</th>
                        <th>{{ 'page_type.label'|trans }}</th>
                        <th>{{ 'actions'|trans }}</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for pageType in pageTypes %}
                        <tr>
                            <td>{{ pageType.id }}</td>
                            <td>{{ pageType.name }}</td>
                            <td>
                                <a href="{{ path('sherlockode_acb_page_type_edit', {id: pageType.id}) }}">{{ 'edit'|trans }}</a>
                                | <a href="{{ path('sherlockode_acb_page_type_delete', {id: pageType.id}) }}">{{ 'delete'|trans }}</a>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
{% endblock %}
