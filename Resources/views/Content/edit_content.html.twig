{% extends '@SherlockodeAdvancedContent/layout.html.twig' %}

{% trans_default_domain 'AdvancedContentBundle' %}

{% block body %}
    {{ parent() }}
    <div class="container edit-content">
        <div class="row page-info">
            <div class="col-md-12">
                <h1>{{ 'content.edit'|trans }}</h1>
                <a href="{{ path('sherlockode_acb_content_list') }}" class="float-end">{{ 'back_to_list'|trans }}</a>
            </div>
        </div>
        <hr>
        {% include '@SherlockodeAdvancedContent/Content/_form.html.twig' %}
    </div>
{% endblock %}
